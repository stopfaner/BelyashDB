#include "collection.h"

// storage::Collection::Collection()

bool storage::Collection::create_metric(std::string name, std::string tag_name) {
    // this->metric_manager->create_metric(this->collection_uuid, name, tag_name);
}