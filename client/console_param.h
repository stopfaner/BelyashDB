#pragma once
#include <vector>

static std::string addr_param = "-a";
static std::string port_param = "-p";
static std::string help_param = "-h";

static std::string params[] = {
    addr_param,
    port_param,
    help_param
};
